name: 2nd action test
on: workflow_dispatch
jobs:
 test: 
  runs-on: ubuntu-latest
  steps: 
   - name: get code
     uses: actions/checkout@v5

   - name: install Node js
     uses: actions/setup-node@v4
     with:
      node-version: 18

   - name: install dependencies
     run: npm ci

   - name: run tests
     run: npm test

 deploy:
  runs-on: ubuntu-latest
  needs: test
  steps:
   - name: get code
     uses: actions/checkout@v5

   - name: install Node js
     uses: actions/setup-node@v4
     with:
      node-version: 18

   - name: install dependencies
     run: npm ci

   - name: run tests
     run: npm test

   - name: build the project
     run: npm run build

   - name: deploy
     run: echo "deploying the program build to the server"

   - name: Patrick Mendoza
     run: echo "watching tutorial for github actions"

